{
  "name": "pace-crm-api",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "cd packages/shared && npm install && cd ../../apps/api && npm install --production=false && npm run build",
    "start": "node apps/api/dist/main.js"
  }
}

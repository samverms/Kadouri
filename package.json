{
  "name": "pace-crm",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "cd packages/shared && npm install && npm run build && cd ../../apps/api && npm install --production=false && npm link ../../packages/shared && npm run build && cd ../web && npm ci --legacy-peer-deps && npm link ../../packages/shared && npm run build && cp -r public .next/standalone/apps/web/ && cp -r .next/static .next/standalone/apps/web/.next/",
    "start": "node start-server.js"
  }
}

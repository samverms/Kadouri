{
  "name": "kadouri-crm",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "if [ \"$APP_TYPE\" = \"api\" ]; then cd packages/shared && npm install && npm run build && cd ../../apps/api && npm install && npm run build; elif [ \"$APP_TYPE\" = \"web\" ]; then cd packages/shared && npm install && npm run build && cd ../../apps/web && npm ci --legacy-peer-deps && npm run build; else echo 'APP_TYPE not set'; fi"
  }
}

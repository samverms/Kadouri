{
  "name": "kadouri-crm",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "cd packages/shared && npm install && npm run build && cd ../../apps/web && npm ci --legacy-peer-deps && npm link ../../packages/shared && npm run build && cd ../api && npm install && npm link ../../packages/shared && npm run build",
    "start": "cd apps/api && npm start"
  }
}

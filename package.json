{
  "name": "kadouri-crm",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "heroku-postbuild": "cd packages/shared && npm install && npm run build && cd ../../apps/web && npm ci --legacy-peer-deps && npm link ../../packages/shared && npm run build && cd ../api && npm install && npm link ../../packages/shared && npm run build",
    "start": "concurrently \"npm run start:api\" \"npm run start:web\"",
    "start:api": "cd apps/api && PORT=3001 npm start",
    "start:web": "cd apps/web && PORT=$PORT npm start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}

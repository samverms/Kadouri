[build]
builder = "NIXPACKS"
buildCommand = "cd packages/shared && npm install && npm run build"

[deploy]
startCommand = "echo 'Use service-specific commands'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
